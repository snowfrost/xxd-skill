---
name: xxd-panel-004
description: "Create XXD Panel 004 artwork from a theme, place, address, coordinates, or reference photo in four combinable modes: reality scene above/precise line design below, scene left/design right, design alone, or a four-device wallpaper pack. Builds or preserves one credible everyday scene, then distils its recognisable skyline, street interface, signage rhythm, structures, people, or subject identity into fine single-line geometry with theme-specific restrained colour and orderly travel-poster typography. Use for the exact 004 collectible modern place-and-theme poster style; never use it for postcard clichés, invented location facts, decorative doodles, generic tourism, 3D, or multi-photo collage."
---

# XXD Panel 004 · 在地现实与现代线描

Turn each supplied theme or reference into one credible, coherent reality scene and a refined graphic-line interpretation of that same scene. Paired modes show the reality scene; design-only and wallpapers show only the line design. For a real place, preserve verified spatial relationships, local text systems, street interfaces, facilities, materials, and everyday rhythm. For a non-place theme, preserve its defining identity, structure, gesture, relation, and visual memory.

Operational rules follow the shared XXD Panel workflow contract: four combinable modes; source-adaptive ordinary canvases; exact 50/50 paired geometry; linked or independent four-device wallpapers; copy and locale preflight; fresh generation jobs; privacy-preserving raster generation; and one fresh task directory per source and mode. Style-specific sections may refine aesthetics, copy hierarchy, and acceptance criteria but never override this contract.

## Non-negotiable contract

- One theme or reference set may be processed in one or more selected modes. Each selected ordinary mode (`top-bottom`, `left-right`, `design-only`) produces one PNG; selected `wallpaper-pack` produces exactly four separate PNGs. Selecting all four modes therefore produces seven final files per input. Keep every mode in its own task directory and never combine modes, inputs, or wallpapers into a grid, contact sheet, or overview.
- Input may be a theme alone or may include a place name, address, coordinates, factual notes, or reference images. Reference images are authoritative visual evidence. A theme-only task first establishes one fresh reality-scene anchor; every selected mode in that invocation derives from that same approved anchor unless the user requests independent interpretations.
- Resolve a non-empty ordered set of modes before generation: `top-bottom`, `left-right`, `design-only`, and/or `wallpaper-pack`. Accept one choice, multiple choices separated by `+`, Chinese/English commas or whitespace, natural-language names, or `全部` / `all`. Deduplicate repeated choices and execute in menu order 1→4. If none is specified, ask with the multiline multi-select menu in the workflow. Do not ask again when the set is already clear.
- When the selected set contains `wallpaper-pack`, require a second choice: `linked` or `independent`. A linked pack approves one line-design anchor wallpaper, iPad by default, and makes every other device reference both the same reality-scene anchor and that line-design anchor. An independent pack gives every device only the same reality-scene anchor. Neither permits resizing or cropping one wallpaper into the others.
- Paired modes split exactly 50/50. `design-only` and every wallpaper contain no visible reality scene, no seam, and no reserved photographic panel.
- Exact user pixels have highest priority, followed by an explicit ratio or destination. With a reference image or approved reality anchor, ordinary modes adapt to the complete source: `top-bottom` = `W×2H`, `left-right` = `2W×H`, and `design-only` = `W×H`. A theme-only task must resolve a canvas before generating its anchor. Never impose the archived initial brief's 3:4 canvas as a silent default.
- A supplied visible photograph remains faithful. Allow only restrained editorial grading and environmental extension needed for an explicitly overridden canvas. A generated reality scene remains faithful to verified theme facts and must be a single unified slice of life, not a collage of attractions.
- The transformed frame must preserve at least three scene- or theme-specific cues while converting the same reality anchor into fine accurate single-line geometry, clear perspective, ordered detail rhythm, restrained whitespace, and limited theme-derived colour. It is not mechanical tracing, decorative doodling, generic skyline clip art, cartoon, 3D render, or unrelated flourish.
- Copy has no silent default. Resolve automatic copy, exact custom copy, or text-free output before image generation. Automatic and custom modes also require a target language or locale. By default, one source-specific copy package is shared verbatim across all selected modes; explicit per-mode copy instructions override it.
- Render no logo, watermark, signature, colour swatch, UI, device mockup, decorative pseudo-text, or unrelated explanatory prose.

## Aesthetic motive lock

Modes and device ratios may change placement and aspect ratio but never the 004 motive. Every transformed frame must visibly express this chain:

**one verified, coherent, everyday reality scene anchored in the user's theme → lock its most recognisable subject, skyline, street interface, sign rhythm, transport element, shopfront relation, human state, or structural memory → distil rather than trace → rebuild with fine accurate single line, clear geometric perspective, ordered detail rhythm, and measured abstraction → use a theme-specific monochrome or very limited palette derived from climate, light, time, material, history, commerce, food memory, nature, and mood → integrate an assertive title and disciplined supporting information as part of the poster structure**.

The sense of place comes from spatial and cultural evidence, not from collecting famous attractions or adding generic lanterns, taxis, cafés, flags, food icons, or postcard sunsets. Modernist line posters, collectible travel editions, city-brand systems, art publishing, and experimental editorial design are construction influences rather than templates.

Reject the result as generic when another city or theme could replace the input without materially changing the reality scene, line structure, perspective, local text, palette, or copy. Also reject landmark collage, exaggerated monuments, tourist-brochure gloss, postcard clichés, fabricated signage, decorative doodles, smooth icon packs, dense colour fill, 3D volume, and unrelated brand styling.

## 004 visual system

### Credible reality anchor

- When the user supplies a photo, address, coordinates, or factual reference, treat it as evidence and never substitute another place or generic scene.
- For a city or location, lock spatial layout, building interface, street width and direction, shopfront/sign rhythm, transport facilities, human activity, local script, materials, climate, and time of day only where supported.
- For a broad city theme, compose one credible slice of everyday life where people, buildings, streets, signs, windows, facilities, food, commerce, and activity coexist naturally. Do not paste separate attractions together.
- People keep pose, direction, clothing mass, and relational distance; animals keep body rhythm and head direction; plants keep growth gesture; architecture keeps skyline and defining openings; objects and vehicles keep functional silhouette and negative shape; landscapes keep one source-specific terrain or spatial relation.
- For a non-place theme, lock the principal identity, silhouette, structure, gesture, relation, and visual memory. In every case, approve one reality-scene anchor before line extraction.

### Distilled line composition

- Select the anchor's most recognisable skyline, façade rhythm, road geometry, signage cadence, transport fixture, window relation, people state, or subject contour.
- Rebuild it with fine accurate single line, clear geometric perspective, measured abstraction, controlled whitespace, and an ordered rhythm of detail. Do not mechanically trace every edge.
- A few horizontal baselines, vertical construction lines, or geometric guides may clarify perspective and rhythm. Every auxiliary element must serve structure.
- Keep one hierarchy and one reading path. Avoid clutter, arbitrary curves, icon scattering, literal miniature maps, and decorative enrichment.

### Fine precise line and tonal rhythm

- Keep line predominantly fine, crisp, and accurate, with subtle density changes and occasional local weight for hierarchy.
- Use perspective, overlap, line spacing, blank-to-mark ratio, and selective darkening to create richness without realistic shading.
- Preserve a clean print or lightly tactile paper character. Avoid shaky expressive wire, thorns, knots, sketch noise, comic outlines, photoreal light, 3D modelling, and glossy vector effects.

### Theme-specific restrained colour

- Derive colour from the theme's climate, light, time, architecture, material, history, commerce, food memory, nature, and emotional texture.
- Prefer monochrome print aesthetics or a very small restrained palette. Vary density, value, local weight, whitespace, and detail rhythm rather than filling many regions.
- Do not impose a fixed palette, postcard sunset, national-flag scheme, generic retro beige, rainbow colour, or unrelated city-brand colour.

## Copy and typography belong to 004

004 typography behaves like a disciplined collectible travel poster or city-brand system, integrated with the scene and line structure rather than pasted on afterward.

- Automatic copy uses one clear principal title from the user's theme. Supporting text may use the theme's original wording, native local language, verified country/region, address, coordinates, number, year, or one short factual note.
- Never invent addresses, coordinates, years, local spellings, landmarks, or slogans. Street and storefront text visible in the scene must be accurate, natural, legible, and professionally spelled.
- Preserve exact user wording verbatim. Refine an editable direction only within permission while protecting audience, mandatory terms, tone, and semantic phrase breaks.
- Apply the unrelated-image swap test: generic lifestyle words that survive on another source must be rewritten.

Typography has its own 004 construction:

- **Role:** title establishes identity; supporting text supplies verified place or edition context.
- **Voice:** clear, modern, quiet, cultured, and locally credible.
- **Construction:** one prominent title with a disciplined hierarchy of zero to three smaller elements; adapt proportions and line breaks to the resolved script.
- **Placement:** integrate title, line drawing, baselines, and margins into one grid. Never cover the key scene identity or float as an afterthought.
- **Material:** clean print, fine ink, or subtly tactile letterpress consistent with the line system.
- **Script:** use a native hand-made editorial equivalent for the resolved locale. Preserve natural Chinese, Japanese, Korean, Arabic, and Latin construction rather than imitating Latin handwriting across every script.

Resolve locale independently from command language:

```text
target market or audience > requested output language > direction language; if none is explicit, ask before generation
```

Localise by transcreation using native wording, rhetoric, punctuation, spacing, shaping, and semantic line breaks. Never infer nationality or audience language from appearance, clothing, scenery, filenames, metadata, or signs.

## Raster generation and privacy

Use the host's built-in bitmap image-generation capability by default and follow the available `imagegen` skill when exposed. View each local reference before generation. Use one generation call per distinct asset; a theme-only task also needs its own reality-scene anchor call, and a wallpaper pack requires four separate wallpaper calls. “Single-line”, “geometric”, and “editorial poster” describe appearance only and never authorise SVG, HTML, CSS, Canvas, diagrams, or programmatic drawing as the artwork.

If built-in bitmap generation is unavailable, use `scripts/configured_imagegen.py`, which reuses an already configured compatible bitmap route and emits sanitised status only:

```bash
python3 scripts/configured_imagegen.py probe
python3 scripts/configured_imagegen.py edit --image source.png \
  --prompt-file /private/job-temp/transform-prompt.txt \
  --out /private/job-temp/design.png --size 1536x1024 --quality high
```

Judge readiness by actual bitmap capability, not a provider name or one environment variable. Never display, log, persist, or report providers, endpoints, headers, credentials, account identifiers, route configuration, prompts, or response bodies. Do not manually inspect credential files. The bridge's sanitised JSON is the complete allowed diagnostic surface.

An explicit invocation of `/xxd-panel-004` or `$xxd-panel-004` with a theme or reference and requested output authorises generation through any already configured authenticated bitmap route available to the session. Do not ask for a second confirmation solely because the route changes. This does not authorise changing credentials, providers, billing, accounts, or global settings.

Only report generation as unavailable after both built-in capability and the bundled probe fail to establish a usable bitmap route. State the limitation narrowly and never guess the cause or identify the provider. Never substitute code-rendered art.

## Fresh-task and source boundary

Every invocation starts a fresh generation job unless the user explicitly asks to continue, audit, compare, edit, or reuse a named earlier result. Repeating the same theme, reference, and parameters still means generating a new reality anchor and new result in a new task directory. An old file can never satisfy a new request.

Use only the current theme, images attached to the current invocation, explicit user paths, explicitly named factual resources, or a previously supplied image that the current request clearly identifies as “the same image”. Never broadly scan the Desktop, workspace, `~/Desktop/xxd/xxd-panel-004/`, or historical task folders to find a substitute reference or anchor.

## Workflow

1. If no mode is resolved, ask exactly this as normal multiline chat text and wait:

   ```text
   请选择一个或多个模式（回复序号；多选可用 +、顿号或逗号）：

   1. 上下双联（现实场景＋同尺寸线描设计）
   2. 左右双联（现实场景＋同尺寸线描设计）
   3. 纯设计版（只显示线描设计）
   4. 四端壁纸套装
      手机＋iPad＋电脑＋儿童手表

   有参考图时，前三种不指定尺寸按参考图自适应；只有主题时请同时给比例或尺寸。壁纸套装可按设备分别给分辨率。
   示例：1｜1+3｜1、2、4｜全部
   ```

2. If the selected set contains `wallpaper-pack` without a relationship, ask and wait:

   ```text
   请选择壁纸关系（回复序号即可）：

   1. 连贯套装（推荐）
      先生成 iPad 定调图；其他三张参考现实场景定调图＋同一线描定调图分别重构
   2. 四张独立
      每张只参考同一现实场景定调图，构图变化更自由
   ```

3. Before generation, resolve copy mode and locale. If missing, ask and wait:

   ```text
   正式做图前，请确认文字设置（回复序号即可）：

   1. 自动文案
      我根据主题、可靠地点信息与 004 气质创作标题；请同时注明语言或地区
   2. 自定义文案
      请直接输入要呈现的准确文字，并注明语言或地区
   3. 无文字

   示例：1｜日语
   示例：2｜英式英语｜STILL IN BLOOM
   ```

4. Resolve dimensions independently for every selected mode: exact pixels > explicit ratio/destination > source adaptation. Exact `top-bottom` height and `left-right` width must be even; never silently round. In multi-select, custom sizes must be labelled by mode; ask when an unlabeled size is ambiguous. Wallpaper-pack has no silent size default. If unresolved, ask:

   ```text
   请选择壁纸尺寸（回复序号即可）：

   1. 常用设备预设
      手机 1440×3200｜iPad 2048×2732｜电脑 3840×2160｜儿童手表 1024×1024
   2. 自定义分辨率
      请分别输入手机、iPad、电脑、儿童手表的尺寸
   ```

5. Open one fresh job boundary and reserve the next unused sibling output directory for every input and selected mode. Confirm only the current theme, user-supplied references, and explicitly named factual resources. Inspect each image separately and read the full prompt matching the working language:
   - Chinese: [references/xxd-panel-004-prompt.zh-CN.md](references/xxd-panel-004-prompt.zh-CN.md)
   - English: [references/xxd-panel-004-prompt.en.md](references/xxd-panel-004-prompt.en.md)
6. Resolve the input route. With a reference photo, use it as the reality anchor. With a theme only, first resolve an anchor canvas, then generate and visually approve one fresh coherent reality scene before any line design. For real places, use supplied address, coordinates, references, and reliably available facts; if exact current spatial truth cannot be established, narrow the scene rather than invent storefronts, signs, or landmarks.
7. Privately lock: the reality anchor; at least three identity cues; principal subject or scene relation; skyline/street/façade/sign/transport/window/people hierarchy; geometric perspective; line-density rhythm; construction-line plan; theme-specific monochrome or restrained palette; title grid; and every verified place-text field. Do not invent unsupported address, coordinates, local spelling, year, landmark, biography, event, or claim.
8. Lock one copy package per input and share it verbatim across selected modes by default; honour explicit per-mode overrides. Priority: explicit text-free request > exact user wording > editable user direction > theme- or source-derived automatic copy. Use one title plus zero to three supporting elements only when useful. Do not reuse one input's copy for another unless asked.
9. Run `scripts/compose_panel.py --plan` for every selected mode and resolved canvas. Selected wallpaper-pack uses four `design-only` plans.
10. Generate each distinct line design alone at the planned aspect and preferably exact frame size, always referencing the approved reality anchor. For paired modes, prepare that reality scene separately. Never ask the model to generate both halves at once.
   Within the same current invocation, ordinary modes may reuse one newly approved transformed intermediate only when frame dimensions, aspect ratio, source facts, aesthetic instructions, typography, and locked copy are identical. Otherwise generate separately. Wallpaper assets are always generated separately.
11. For `independent`, generate four wallpapers directly from the same reality anchor. For `linked`, generate and visually approve the iPad line-design anchor first; every derivative receives exactly the reality anchor plus that same line-design anchor. The reality anchor locks facts and identity; the line anchor locks only abstraction grammar, perspective discipline, line weight, palette, whitespace, and typography. Never chain derivatives.
12. Finalise every selected mode in menu order with `scripts/compose_panel.py`, reopen each PNG at normal and thumbnail size, and apply the acceptance gate. Retry only the faulty generated asset once when a hard invariant fails. After one failed correction, return the best result and name the unresolved issue rather than pretending success.
13. Return absolute paths in input order, then menu order 1→4. Wallpaper order is phone, iPad, desktop, watch.

## Generation payload

Append the resolved mode block to every transformed-asset prompt:

```text
OUTPUT MODE: TOP_BOTTOM | LEFT_RIGHT | DESIGN_ONLY | WALLPAPER_PACK
DEVICE PROFILE: NONE | PHONE | IPAD | DESKTOP | WATCH
FINAL SIZE: <exact WIDTHxHEIGHT>
DESIGN FRAME: <exact WIDTHxHEIGHT>
SOURCE VISIBILITY: UPPER PANEL | LEFT PANEL | REFERENCE ONLY — NOT VISIBLE
LAYOUT RULE: Fill the design frame. Render no extra photo, seam, frame, or reserved photographic area inside the transformed design.
WALLPAPER RULE: Recompose for this device; keep system-UI zones low-information and essential content safe; render no clock, icons, dock, controls, or mockup; never crop another device output.
WALLPAPER RELATIONSHIP: NONE | INDEPENDENT | LINKED
ANCHOR DEVICE: NONE | PHONE | IPAD | DESKTOP | WATCH
REFERENCE ROLE: SOURCE ONLY | SOURCE CONTENT + ANCHOR VISUAL DNA
```

For text output, append:

```text
COPY MODE: REQUIRED
COPY ORIGIN: USER_EXACT | USER_DIRECTION | SOURCE_DERIVED
COPY LOCALE: <resolved locale>
COPY INTENT — INSTRUCTION ONLY, NEVER RENDER: <source-specific meaning and tone>
TITLE: <locked exact string>
NOTE 1: <optional locked exact string>
NOTE 2: <optional locked exact string>
TYPE ROLE: COLLECTIBLE TRAVEL-POSTER / CITY-BRAND HIERARCHY
TYPE PLACEMENT: TOP TITLE | GRID MARGIN | STRUCTURAL BASELINE | OPEN SPACE
TYPE MATERIAL: CLEAN PRINT | FINE INK | SUBTLE LETTERPRESS
TYPE SCALE: PROMINENT TITLE + ZERO TO THREE DISCIPLINED SUPPORT ELEMENTS
COPY RULE: Render only TITLE and populated NOTE strings, each exactly once. Instruction fields are never visible. Do not rewrite, translate, spell-correct, duplicate, or add text. Use native script shaping, punctuation, spacing, and semantic line breaks.
```

Remove unused note lines. For text-free output use only `COPY MODE: NONE — render no text or pseudo-text anywhere.`

## Composition commands

```bash
# Source-adaptive top-bottom
python3 scripts/compose_panel.py --plan --layout top-bottom --source photo.png
python3 scripts/compose_panel.py --source photo.png --design design.png \
  --out result.png --layout top-bottom

# Source-adaptive left-right
python3 scripts/compose_panel.py --plan --layout left-right --source photo.png
python3 scripts/compose_panel.py --source photo.png --design design.png \
  --out result.png --layout left-right

# Design-only
python3 scripts/compose_panel.py --source photo.png --design design.png \
  --out result.png --layout design-only

# One wallpaper, repeated separately for all four devices
python3 scripts/compose_panel.py --design phone.png --out wallpaper-phone.png \
  --layout design-only --size 1440x3200
```

`--size WIDTHxHEIGHT` overrides `--canvas` and source adaptation. Generate the design at the exact planned aspect; if exact pixels are unsupported, use a closest supported same-aspect bitmap and resample proportionally without cropping. Audit with the same layout used to build the result.

## Output location

Save finished work under `~/Desktop/xxd/xxd-panel-004/` unless the user supplies another destination. Create the shared `~/Desktop/xxd/` wrapper, the skill root, and each task directory when needed.

- Wrap every source-and-mode result in a fresh task directory: `<source-stem>-top-bottom/`, `<source-stem>-left-right/`, `<source-stem>-design-only/`, or `<source-stem>-wallpaper-pack/`.
- A batch or multi-select creates one sibling task directory per source and selected mode. Never mix sources or modes.
- Ordinary task directories contain only one finished PNG: `<source-stem>.png`, `<source-stem>-lr.png`, or `<source-stem>-design.png`.
- The final count per source equals one file for each selected ordinary mode plus four files when `wallpaper-pack` is selected. `all` / `全部` therefore means seven final PNGs across four sibling task directories.
- A wallpaper task directory contains exactly four finished PNGs named `<source-stem>-wallpaper-phone.png`, `-ipad.png`, `-desktop.png`, and `-watch.png`; do not create device subdirectories.
- Never overwrite. Append `-2`, `-3`, and so on to a colliding task-directory name while keeping filenames unchanged.
- Keep prompts, intermediate generations, plans, audits, and source copies outside the finished task directory.

## Acceptance gate

Before accepting each result verify:

- Mode, exact pixels, source-adaptive formula, split axis, seam, and output count are correct.
- A supplied photograph remains faithful; a generated reality anchor is one coherent credible scene with no landmark collage, generic postcard staging, or unsupported local fact. Source-hidden outputs contain no reality panel or seam.
- The line design remains recognisable through at least three anchor-specific cues and preserves the principal spatial, structural, gestural, or narrative relation.
- Fine accurate line, clear geometric perspective, ordered detail rhythm, measured abstraction, and controlled whitespace coexist; it is neither mechanical tracing nor decorative doodling.
- Skyline, street interface, sign rhythm, transport, window, people, or non-place subject features are selected because they identify this theme, not because they are generic travel symbols.
- Horizontal, vertical, and geometric construction marks all serve perspective or rhythm. There is no icon scatter, dense scene copying, comic outline, photoreal shading, glossy vector, or 3D model.
- The theme-specific monochrome or very limited palette is traceable to climate, light, time, materials, history, commerce, food memory, nature, or mood—not a fixed template.
- Automatic copy is concise, grounded, and 004-specific; exact user wording remains verbatim. All rendered text is accurate and native to the resolved locale; text-free output contains no text or pseudo-text.
- Typography has one clear title and only useful verified supporting information. Local language, address, coordinates, year, signage, and spelling are accurate; hierarchy is integrated with the line grid rather than pasted on.
- Every wallpaper is separately recomposed, respects safe regions, contains no system UI, and is not a crop of another device result. A linked pack shares one family without drifting from the source.
- Every delivered PNG was newly generated for this invocation and lives in its fresh task directory.

## Override policy

Preserve user-specified source, mode set, output count, dimensions, target locale, copy mode, and exact finished wording. Priority is explicit text-free request > exact user wording > editable user direction > source-derived automatic copy. Exact pixels override ratio or destination; ratio or destination overrides source adaptation. A labelled wallpaper size overrides only that device.

User instructions may change subject emphasis, scene moment, abstraction level, line density, palette, or copy within 004, but do not silently relax one-input isolation, exact paired geometry, four separate wallpaper outputs, fresh-task generation, source-hidden output rules, factual place integrity, or native-language typography. Leave the credible-reality-plus-modern-line-poster aesthetic only when the user explicitly asks to leave the 004 style.

## Provenance boundary

The user's original style brief is archived at [references/004-source.md](references/004-source.md). It records the initial 3:4 top-bottom example but does not override the operative mode and source-adaptive sizing rules. The full local 004 prompt is the production specification. Never borrow subjects, colours, copy, or compositions from samples or previous outputs.
