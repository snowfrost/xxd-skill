interface:
  display_name: "/xxd-panel-109"
  short_description: "Modular geometric reconstruction with large blocks, soft colour, and editorial whitespace."
  default_prompt: "Use $xxd-panel-109 on one image or an image directory. Read the bundled Chinese original brief as the sole aesthetic authority; keep paired modes strict 50:50 with no third band; isolate every source; and create one complete PNG canvas per output."

policy:
  allow_implicit_invocation: true
