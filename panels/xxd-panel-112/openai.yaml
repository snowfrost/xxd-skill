interface:
  display_name: "/xxd-panel-112"
  short_description: "Quiet handmade-paper collage with matte crayon and restrained gold accents."
  default_prompt: "Use $xxd-panel-112 on one image or image directory. Treat its Chinese original brief as the sole aesthetic authority, keep paired modes strict 50:50 with no third band, isolate every source, and create fresh complete-canvas PNG outputs."

policy:
  allow_implicit_invocation: true
