interface:
  display_name: "/xxd-panel-105"
  short_description: "Turn images into poetic minimalist paper-collage memories."
  default_prompt: "Use $xxd-panel-105 to turn one image or an image directory into poetic minimalist paper-collage artwork. Keep the bundled Chinese original brief as the sole aesthetic authority, resolve shared batch settings once, isolate every source, send exactly one mode-specific delivery contract per asset, keep one flat task directory, make design-only and wallpaper outputs fully transformed, and ground factual-looking copy in supplied or verified facts."

policy:
  allow_implicit_invocation: true
