interface:
  display_name: "/xxd-panel-110"
  short_description: "Japanese lifestyle scene maps with realistic objects, acrylic edges, gentle labels, and relaxed editorial rhythm."
  default_prompt: "Use $xxd-panel-110 on one image or an image directory. Read the bundled Chinese original brief as the sole aesthetic authority; keep paired modes strict 50:50 with no third band; isolate every source; and create one complete PNG canvas per output."

policy:
  allow_implicit_invocation: true
