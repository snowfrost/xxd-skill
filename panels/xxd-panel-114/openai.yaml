interface:
  display_name: "/xxd-panel-114"
  short_description: "Pastel scrapbook doodles with retro paper and active whitespace."
  default_prompt: "Use $xxd-panel-114 on one image or image directory. Treat its Chinese original brief as the sole aesthetic authority, keep paired modes strict 50:50 with no third band, and create fresh complete-canvas PNG outputs."

policy:
  allow_implicit_invocation: true
