interface:
  display_name: "/xxd-panel-111"
  short_description: "Hand-sewn textile collage with visible stitches and warm editorial whitespace."
  default_prompt: "Use $xxd-panel-111 on one image or image directory. Treat its Chinese original brief as the sole aesthetic authority, keep paired modes strict 50:50 with no third band, isolate every source, and create fresh complete-canvas PNG outputs."

policy:
  allow_implicit_invocation: true
